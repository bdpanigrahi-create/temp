{
  "model_armor_template": "sre-log-shield",
  "filters": {
    "prompt_injection_protection": {
      "confidence_threshold": "HIGH",
      "action": "BLOCK_AND_REDACT"
    },
    "pii_redaction": {
      "active": true,
      "entities": ["CREDIT_CARD", "PASSWORD", "PRIVATE_KEY", "API_KEY"]
    },
    "shell_code_safety": {
      "block_unauthorized_commands": true,
      "forbidden_commands": ["rm -rf /", "wget", "curl", "chmod +x"],
      "action": "FILTER_OUTPUT"
    }
  }
}
